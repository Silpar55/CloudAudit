/**
 * CloudAudit — Authenticated app route: `$slug.tsx`.
 * Requires login; lives under the main app shell (sidebar/header).
 */

import { AwsServiceDetailPage } from "~/components/teams";

export default AwsServiceDetailPage;
